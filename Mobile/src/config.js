// Fallback used the very first time the app runs, before anyone has saved
// a custom URL from the API Settings screen (accessible from the Login
// screen). Update this if your default dev machine's address changes.
export const DEFAULT_API_BASE_URL = 'http://localhost:8000/api'
